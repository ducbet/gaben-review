$("<%= j render(@reply) %>").insertBefore("#reply_form_" + <%= @reply.review.id %>);
$("#reply_" + <%= @reply.review.id %> +"_text_field").val("");
$("#reply_" + <%= @reply.review.id %> +"_text_field").attr("placeholder", "");

